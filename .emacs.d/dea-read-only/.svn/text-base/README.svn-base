安装
    apt-get install gnuserv
    svn checkout http://dea.googlecode.com/svn/trunk/ dea-read-only
    cd dea-read-only
    ./install.sh
    安装的时候, 会先根据.emacs文件生成一个.emacs.changed文件,因为.emacs
    文件中会用到一些与.emacs文件所在文件夹相关的变量,然后在HOME目录建立
    一个.emacs.changed的软链接(ln -sf CUR_DIR/.emacs.changed ~/.emacs), 
    如果HOME目录下已经存在.emacs文件, install.sh会把它备份到~/.emacs.CURRENT_DATE_TIME.
    所以, 如果你更新的时候是用svn up更新的话,还要执行一下 $ install.emacs.sh, 
    重新生成HOME下的.emacs.另外install.sh还会解压lisps目录下的压缩包.

更新
    cd dea-read-only
    ./update.sh

修改
    修改完.emacs后需要运行 $ ./install.emacs.sh, 或者退出emacs的时候, 
    会自动运行$ ./install.emacs.sh

目录结构说明
lisps:
    存放从网上下载下来的扩展, 有些我经过略微修改, 在原有扩展名前加上"my-"前缀
mode-abbrevs:
    从msf-abbrev略微修改出来的my-msf-abbrev扩展,这个扩展的一些缩写就放在这个目录
my-lisp:
    我自己写的或修改别人的扩展
templates:
    该文件夹存放template.el所需要的一些模板文件

my-lisps文件夹中扩展说明
command-frequence.el (改自command-frequency.el)
    查看命令和按键的频率,定义按键的时候非常有用
describe-symbol.el and find-symbol.el (原创)
    非常方便的查看命令,函数,变量的一个扩展
select-buffer.el (改自wcy-swbuff.el, http://smth.edu.cn/pc/pccon.php?id=2525&nid=49359)
    可以像windows下Alt-tab那样切换窗口
edit-rdonly.el (原创)
    在只读状态下进行编辑, 主要是只读状态下浏览文件时, 不需要退出只读状态, 直接进行
    删除一行, 粘贴一行等非字符级的编辑操作, 非常方便.
每个扩展的详细信息请参考扩展文件本身

其他说明
1. 颜色配置
   颜色配置都放在my-lisps/my-face-settings.el和my-lisps/my-face.el里面, 
   my-face-settings.el文件里面配置各种颜色,my-face.el里面则是我定义的一些颜色.
   (require 'my-face-settings)就可以在你的emacs里面使用我的颜色配置.

2. view mode
   M-s进入view mode, 既可以非常方便的浏览文件, 又可以进行非字符级的编辑操作, 还支持一些
   vim命令模式下的命令, 比如j,k,l,h,A等, 非常方便, q退出view mode.

3. 非常方便的编辑功能
   1. M-w smart-copy
      region状态下,拷贝region,否则拷贝当前行, 还可以拷贝矩阵region

   2. C-k
      删除一行
      M-K
      删除当前光标到行尾

   3. C-w
      在region状态下, 剪切当前region, 否则向后删除一个单词, 非常之方便

   4. C-x M-w
      复制一行并粘贴到下一行
      M-W
      复制region并粘贴到下面
      C-x M-W
      复制一个sexp并粘贴到下面
      相信很多程序员都有拷贝现成代码并复制下来,然后经过小的修改(当然这种方法不是一个好的方法), 
      这时候这个命令就显的非常有用了

   5. C-x w
      复制一个sexp
      C-x W
      删除一个sexp
      M-D
      删除当前光标所处的单词
      C-x S
      选择一个sexp
